import { FC } from 'react';
import { NotReady } from '@/widgets';
import './styles.css';

export const Services: FC = () => {
  return (
    <div className='services-page'>
      <NotReady />
    </div>
  );
};
